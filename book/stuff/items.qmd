---
title: "Items"
execute:
    echo: false
    output: asis
---

```{python}
import os 
from citutils import (entity_text_generators as ge,
    parsers as tu)

text = ""
entities = tu.get_entities(os.path.join("..","..","data","entities"), True)

print("## Basic Items (No Spoilers) \n" + 
    ge.filter_generate_text(entities, 
    filter_tags_include = "item, basic",
    filter_tags_exclude = "",
    html_characters = True))

print("## The Rest of the Items (Spoilers) \n" + 
    ge.filter_generate_text(entities, 
    filter_tags_include = "item",
    filter_tags_exclude = "basic",
    html_characters = True))

```